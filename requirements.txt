# Core data science
pandas==3.0.1
numpy==2.4.3
scipy==1.17.1
scikit-learn==1.8.0

# NLP
sentence-transformers==5.3.0
spacy==3.8.13
# After install, run: python -m spacy download en_core_web_sm

# LLM (google-genai is the current SDK; google-generativeai is deprecated)
google-genai>=1.0.0

# HTTP
requests==2.33.0

# HTML parsing (Erowid ETL)
beautifulsoup4>=4.12
lxml>=5.0

# Vector store
faiss-cpu>=1.7.4

# Clustering
hdbscan>=0.8.33

# Dimensionality reduction (optional — for UMAP visualization)
umap-learn>=0.5.6

# Narrative evolution pipeline
pyarrow>=14.0.0        # parquet I/O for embedding persistence
plotly>=5.20.0         # interactive figures
kaleido>=0.2.1         # static PNG export from Plotly
streamlit>=1.35.0      # dashboard

# Fine-tuned classifier (Member 1 — Task 1)
transformers>=4.41.0   # DistilBERT fine-tuning + inference
datasets>=2.19.0       # HuggingFace Dataset utilities
accelerate>=0.30.0     # TPU/GPU training backend
torch>=2.0.0           # PyTorch (CPU/GPU); for Colab TPU see notebook

# Evaluation metrics (Member 1, 2, 3)
rouge-score>=0.1.2     # ROUGE-L for summary evaluation
bert-score>=0.3.13     # BERTScore for summary semantic similarity
ragas>=0.1.7           # Faithfulness / RAG evaluation
